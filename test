    public static Uri getFileUriFromPath(Context cont, String path) {
        if (cont == null) return null;
        File outFile = new File(cont.getFileStreamPath(path + WatchFaceConstant.ZIP_EXTENSION).getPath());
        return (outFile.exists()) ? Uri.fromFile(outFile) : null;
    }
